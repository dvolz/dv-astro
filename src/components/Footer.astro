---
import CatSVG from '../components/CatSVG.astro'
import AstroSVG from '../components/AstroSVG.astro'
const currentYear = new Date().getFullYear()
import Social from './Social.astro'
const socialLinks = [
	{ platform: 'twitter', username: 'johndoe', mdicon: 'twitter' },
	{ platform: 'instagram', username: 'penia', mdicon: 'instagram' },
	{ platform: 'linkedin', username: 'davidtvolz', mdicon: 'linkedin' },
]
---

<style lang="scss">
	footer {
		padding: 4rem 3rem;
		position: relative;
		&:before,
		&:after {
			bottom: inherit;
			top: 0;
			width: calc(100% - 4rem);
			left: 2rem;
		}
	}
	.row {
		display: flex;
		flex-flow: row wrap;
		width: 100%;
	}
	.column {
		width: 100%;
		text-align: center;
		margin-bottom: 2rem;
	}
	.social-footer {
		display: flex;
		justify-content: center;
		align-items: center;
		padding: 0;
		li {
			list-style-type: none;
		}
	}

	.astroColumn {
		display: flex;
		align-items: center;
		justify-content: center;
		flex-flow: row wrap;
		p {
			margin: 0;
		}
	}

	@media screen and (min-width: 768px) {
		$column-width: 33.33337%;
		.column {
			flex: 0 0 $column-width;
			max-width: $column-width;
		}
		.social-footer {
			justify-content: start;
		}
		.astroColumn {
			justify-content: end;
		}
	}
</style>
<footer class="solo-line">
	<div class="row">
		<div class="column">
			<ul class="social-footer">
				{
					socialLinks.map(({ platform, username, mdicon }) => (
						<Social platform={platform} username={username} mdicon={mdicon} />
					))
				}
			</ul>
		</div>

		<div class="column catColumn">
			<CatSVG />
			<p>&copy; Copyright {currentYear}</p>
		</div>
		<div class="column astroColumn">
			<p>Developed from scratch using</p>
			<AstroSVG />
		</div>
	</div>
</footer>
